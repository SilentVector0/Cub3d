
void	check_map(char **grid)
{
	int	i;
	int	j;

	while (grid[j])
	{
		while (grid[j][i])
		{
			if (grid[j][i] = 0 && grid[j + 1][i]
				&& grid[j - 1][i] && grid[j][i + 1]
				&& grid[j][i - 1] =! 0)
			{

			}
			i++
		}
	}
}